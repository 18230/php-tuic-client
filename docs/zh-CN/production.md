# 生产部署说明

`php-tuic-client` 目前还不是可直接投产的版本。当前阶段刻意只包含：

- 配置解析
- doctor 自检
- dry-run 校验
- 发布和 CI 脚手架

现阶段更推荐这样用：

1. 先把 TUIC 配置模型定下来
2. 在 CI 里用 `doctor` 和 `run --dry-run` 做校验
3. 等传输运行时实现后，再作为真正的本地代理长驻进程部署
